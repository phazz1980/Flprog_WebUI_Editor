/** Размер клетки сетки (логич. ед.). В клиенте сетка не рисуется. */
export const GRID_SIZE = 5;

/** Пикселей на одну логическую единицу канвы — как в редакторе. Используется, когда масштаб не «подгонка под экран». */
export const PIXELS_PER_UNIT = 2;
