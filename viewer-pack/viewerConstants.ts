/**
 * Константы для просмотрщика UI (рантайм-отрисовка).
 * См. UI_RENDER_RULES.md, п. 8.
 */

/** Размер клетки сетки в логических единицах (для привязки координат). При «чистой» отрисовке сетка не рисуется. */
export const GRID_SIZE = 5;
