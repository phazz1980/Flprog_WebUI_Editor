****Доработка на новый интерфейс класса Server

****При физическом  отключении модуля и последующем его подключении модуль не переинициализируется!

Доработать Modbus TCP под новй интерфейс объектов Ethernet

****Проверить и интегрировать  RT_HW_Base.device.spi.csETH и RT_HW_Base.device.spi.busETH

****Реализовать повторный запрос на DHCP сервер с заданным периодом

Написать пример для определения пина CS и шины  

Написать описание API библиотеки

****Метод pool у интерфейса должен возвращать результат исполнения (FLPROG_ERROR, FLPROG_SUCCESS,  FLPROG_WITE) 

Реализовать в WizNet проверку задания Mac адреса ( bool FLProgAbstractTcpInterface::checkMac(uint8_t *mac) )

В WiFi интерфейсе реализовать обработку ошибки незаданногo SSID сети

****Подключить в примерах вывод заголовка консоли, но.... через flprogUtilites что бы в компакной версии не падать

****В метод flprog::printConsole(); передавать  текст заголовка

Допилить заглушки в классе flprogEsp8266Wifi.h

Определиться насчет оптимизации работы с сокетами на WizNet

Реализовать задание имени устройства для  

На ESP отладить работу точки доступа при некорректном подключении клиента

При подключении к серверу ESP32  тухнет светодод на втором пине, но контроллер нормально работает

Разобраться с LED_BUILTIN на различных платформах. ESP 32 такая константа не определенна. На ней (node mcu) светодиод - пин 2

*****На расбери не выводится консоль почему то!!!!

*****Не работает перезапрос DHCP (Montain)

Для ESP32  не заданны RT_HW_Base.device.spi.csETH и RT_HW_Base.device.spi.busETH

********Определиться, нужно ли запускать DNS клиетна только при включённой точке доступа WIFI. И если да то в какую сеть пускать запрос. В обе?

Реализовать для  On Board WIFI следующие методы
            uint16_t localPort() 
            IPAddress remoteIP() 
            uint16_t remotePort() 